const defaultConfig = {
  // 打包时按需更改（此项目对应在 github 上的地址：https://dongwanhong.github.io/react-todolist/）
  baseURL: 'http://localhost:3000/',
  timeout: 10000,
  contentType: 'application/x-www-form-urlencoded;charset=UTF-8',
};

export default defaultConfig;
